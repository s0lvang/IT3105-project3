hex = dict(size=4)

general = dict(
    verbose=True,
    starting_player="one",  # one, two or mix
    episodes=10,
    amount_of_players=3,
    c=1,
    rollouts=5,
    epsilon=1,
    epsilon_decay_rate=0.1,
)
